---
title: Yearly Archive
layout: base.njk
permalink: "archives/{% if pagination.pageNumber > 0 %}{{ posts }}/{% endif %}index.html"
pagination:
  data: collections.postsByYear
  size: 1
  alias: posts
  reverse: true
---
<h1>{{ posts | title }}</h1>
<ul>
  {% for post in collections.postsByYear[posts] | reverse %}
  <li>
    {{post.date | readableDate}} <a href="{{ post.url }}">{{ post.data.title }}</a>
  </li>
  {% endfor %}
</ul>


